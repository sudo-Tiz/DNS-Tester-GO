# DNS Tester GO - Environment Variables Example
# Copy this file to .env and adjust values as needed

# API Server Configuration
DNS_TESTER_HOST=0.0.0.0
DNS_TESTER_PORT=5000
CONFIG_PATH=/app/conf/config.yaml

# Redis Configuration (optional - enables distributed task queue)
REDIS_URL=redis://redis:6379/0

# Worker Configuration (when using Redis backend)
MAX_WORKERS=4
CLEANUP_INTERVAL=10

# DNS Query Configuration
DNS_TIMEOUT=5
MAX_SERVERS_PER_REQ=50

# Logging
LOG_LEVEL=info

# Notes:
# - REDIS_URL: If set, enables Redis-backed task queue for async processing
# - MAX_WORKERS: Number of concurrent worker goroutines (default: 4)
# - CLEANUP_INTERVAL: Task cleanup interval in minutes (default: 10)
# - DNS_TIMEOUT: DNS query timeout in seconds (default: 5)
# - MAX_SERVERS_PER_REQ: Maximum DNS servers per API request (default: 50)
